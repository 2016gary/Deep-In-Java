# 数据结构和算法
### 数据结构是一门研究非数值计算的程序设计问题中的操作对象，以及它们之间的关系和操作等相关问题的学科。简单来说数据结构就是数据元素相互之间存在的一种或多种特定关系的集合。
### 传统上，我们把数据结构分为逻辑结构和存储结构。
### 逻辑结构：是指数据对象中数据元素之间的相互关系，也是我们今后最需要关注和讨论的问题。
### 存储结构：是指数据的逻辑结构在计算机中的存储形式。
![](https://i.imgur.com/zVECXoE.png)

---
# 逻辑结构
### 四大逻辑结构：
## 1.集合结构：
### 集合中的数据元素除了同属于一个集合外，它们之间没有其他的关系
![](https://i.imgur.com/EZ6S7rq.png)

## 2.线性结构：
### 线性结构中的数据元素之间是一对一的关系
![](https://i.imgur.com/gWdUAW6.png)

## 3.树形结构：
### 树形结构中的数据元素之间存在一对多的层次关系
![](https://i.imgur.com/66utdoI.png)

## 4.图形结构：
### 图形结构的数据元素是多对多的关系
![](https://i.imgur.com/EooXGPv.png)

---
# 存储结构
### 存储结构实际上研究的是如何把数据元素存储到计算机的存储器中。顺序存储结构和链式存储结构适用在内存结构中。索引存储结构和散列存储结构适用在外存与内存交互结构,像硬盘、软盘、光盘等外部存储器。
### 四大存储结构：
## 1.顺序存储结构：
### 顺序存储结构是把数据元素存放在地址连续的存储单元里，其数据间的逻辑关系和物理关系是一致的。
![](https://i.imgur.com/xmhVvAd.png)

## 2.链式存储结构：
### 链式存储结构是把数据元素存放在任意的存储单元里，这组存储单元可以是连续的，也可以是不连续的。面对时常要变化的结构，顺序存储是不科学的，相比链式存储结构就灵活多了。很显然，链式存储结构的数据元素存储关系并不能反映其逻辑关系，因此需要用一个指针存放数据元素的地址，这样通过地址就可以找到相关联数据元素的位置。
![](https://i.imgur.com/FL86C4X.png)

## 3.索引存储结构：
### 在存储结点信息的同时，还建立了一份附加的索引表。索引表由若干索引项组成。若每个结点在索引表中都有一个索引项，则该索引表称为稠密索引。若一组结点在索引表中只对应一个索引项，则该索引表称为稀疏索引。索引的一般形式为：（关键字、地址）。其中关键字是能够唯一标识一个结点的那些数据项。稠密索引中索引项的地址指示结点所在的存储位置；稀疏索引中索引项的地址指示一组结点的起始存储位置。
![](https://i.imgur.com/XnLiMoq.png)

## 4.散列存储结构：
### 散列存储，又称Hash存储，是一种力图将数据元素的存储位置与关键码之间建立确定对应关系的查找技术。根据结点的关键字直接计算出该结点的存储地址，四种基本存储方法，既可单独使用，也可组合起来对数据结构进行存储映像。同一逻辑结构采用不同的存储方法，可以得到不同的存储结构。选择何种存储结构来表示相应的逻辑结构，视具体要求而定，主要考虑运算方便及算法的时空要求。
![](https://i.imgur.com/qzreUKE.png)

---
# 算法
### 算法是解决特定问题求解步骤的描述，在计算机中表现为指令的有限序列，并且每条指令表示一个或多个操作。

## 算法效率的度量方法
### 事后统计方法：这种方法主要是通过设计好的测试程序和数据，利用计算机计时器对不同算法编制的程序的运行时间进行比较，从而确定算法效率的高低。
### 事前分析估算方法：在计算机程序编写前，依据统计方法对算法进行估算。
### 例如用Python3来写1加到100的代码：
### 第一种算法：
	sum = 0	//执行一次
	n = 100	//执行一次
	for i in range(1,n+1):	//执行n+1次
		sum+=i	//执行n次
	print(sum)	//执行一次
### 第二种算法（高斯算法）：
	sum = 0	//执行一次
	n = 100	//执行一次
	sum = (1+n)*n/2	//执行一次
	print(sum)	//执行一次
### 第一种算法执行了2n+4次，第二种算法执行了4次。

## 算法时间复杂度
### 在进行算法分析时，语句总的执行次数T(n)是关于问题规模n的函数，进而分析T(n)随n的变化情况并确定T(n)的数量级。算法的时间复杂度，也就是算法的时间量度，记作：T(n)= O(f(n))。它表示随问题规模n的增大，算法执行时间的增长率和f(n)的增长率相同，称作算法的渐近时间复杂度，简称为时间复杂度。其中f(n)是问题规模n的某个函数。一般情况下，随着输入规模n的增大，T(n)增长最慢的算法为最优算法。
### 显然由算法时间复杂度的定义可知，上述1加到100的求和算法的时间复杂度分别为O(1)，O(n)。由T(n)增长最慢的算法为最优算法可知，第二种算法（高斯算法）对于1加到100的求和算法要由于第一种算法。
![](https://i.imgur.com/cTgLhVg.png)

## 推导大O阶方法
### 1.常数阶：时间复杂度为O(1)
	sum = 0	//执行一次
	n = 100	//执行一次
	print('Hello World!')	//执行一次
	print('Hello World!')	//执行一次
	print('Hello World!')	//执行一次
	print('Hello World!')	//执行一次
	print('Hello World!')	//执行一次
	sum = (1+n)*n/2	//执行一次
	print(sum)	//执行一次

### 2.线性阶：时间复杂度为O(n)
### 一般含有非嵌套循环涉及线性阶，线性阶就是随着问题规模n的扩大，对应计算次数呈直线增长。
	sum = 0	//执行一次
	n = 100	//执行一次
	for i in range(1,n+1):	//执行n+1次
		sum+=i	//执行n次
	print(sum)	//执行一次

### 3.平方阶：时间复杂度为O(n^2)
	sum = 0	//执行一次
	n = 100	//执行一次
	for i in range(1,n+1):	//执行n+1次
		for j in range(1,n+1):	//执行(n+1)*(n+1)次
			print('Hello World!')	//执行n^2次

### 4.对数阶：时间复杂度为O(logn)
### 2^x>=n的时候停止迭代，得到x=log(2)n次
	i = 1	//执行一次
	n = 100	//执行一次
	while i<n :	//执行log(2)n+1次
		i = i*2	//执行log(2)n次

## 常见的时间复杂度
![](https://i.imgur.com/u11IhTk.gif)
### 常用的时间复杂度所耗费的时间从小到大依次是：O(1) < O(logn) < (n) < O(nlogn) < O(n^2) < O(n^3) < O(2^n) < O(n!) < O(n^n)

## 算法空间复杂度
### 算法的空间复杂度通过计算算法所需的存储空间实现，算法的空间复杂度的计算公式记作：S(n)=O(f(n))，其中，n为问题的规模，f(n)为语句关于n所占存储空间的函数。我们在写代码时，完全可以用空间来换去时间。

## 线性表（List）：由零个或多个数据元素组成的有限序列
### 若将线性表记为（a1,…,ai-1,ai,ai+1,…an）,则表中ai-1领先于ai,ai领先于ai+1,称ai-1是ai的直接前驱元素,ai+1是ai的直接后继元素。线性表元素的个数n(n>=0)定义为线性表的长度，当n=0时，称为空表。
![](https://i.imgur.com/BtzayGl.jpg)

## 设计一门计算机语言为什么需要考虑数据类型呢？
### 数据类型是指一组性质相同的值的集合及定义在此集合上的一些操作的总称。在计算机中，内存不是无限大的，要计算入1+1=2这样的整型数字的加减乘除运算，显然不需要开辟很大的内存空间。而如果要计算1.23456789+2.987654321这样带大量小数的，就需要开辟比较大的空间才存放的下。于是计算机的研究者们就考虑，要对数据类型进行分类，分出多种数据类型来适合各种不同的计算条件差异。

## 抽象数据类型（Abstract Data Type，ADT）
### 是指一个数学模型及定义在该模型上的一组操作
### 描述抽象数据类型的标准格式：
	ADT 抽象数据类型名
	Data
	    数据元素之间逻辑关系的定义
	Operation
	    操作
	endADT

# 排序算法
![](https://i.imgur.com/u6DMSO9.png)
## 冒泡排序BubbleSort
### 步骤：
1. 比较相邻的元素。如果第一个比第二个大，就交换他们两个。
2. 对第0个到第n-1个数据做同样的工作。这时，最大的数就“浮”到了数组最后的位置上。
3. 针对所有的元素重复以上的步骤，除了最后一个。
4. 持续每次对越来越少的元素重复上面的步骤，直到没有任何一对数字需要比较。
### Python3实现：
	arr=[1,7,4,10,6,5]
	temp=0
	
	for i in range(len(arr)):
		for j in range(len(arr)-i-1):
			if arr[j]>arr[j+1]:
				temp=arr[j]
				arr[j]=arr[j+1]
				arr[j+1]=temp
		
	print(arr)

## 选择排序SelectionSort
### 步骤：
1. 在未排序序列中找到最小（大）元素，存放到排序序列的起始位置。
2. 再从剩余未排序元素中继续寻找最小（大）元素，然后放到剩余未排序序列的起始位置。
3. 以此类推，直到所有元素均排序完毕。
### Python3实现：
	arr=[1,7,4,10,6,5]
	min=0
		
	for i in range(len(arr)):
		min=i
		for j in range(i+1,len(arr)):
			if arr[j]<arr[min]:
				min=j
		arr[min],arr[i] = arr[i],arr[min]
			
	print(arr)

## 插入排序InsertionSort
### 步骤：
1. 从第一个元素开始，该元素可以认为已经被排序
2. 取出下一个元素，在已经排序的元素序列中从后向前扫描
3. 如果被扫描的元素（已排序）大于新元素，将该元素后移一位
4. 重复步骤3，直到找到已排序的元素小于或者等于新元素的位置
5. 将新元素插入到该位置后
6. 重复步骤2~5

![](https://i.imgur.com/SANbYVf.gif)
### Python3实现：
	arr=[1,7,4,10,6,5]
	temp=0
	index=0
		
	for i in range(1,len(arr)):
		if arr[i]<arr[i-1]:
			temp=arr[i]
			index=i
			for j in range(i-1,-1,-1):
				if temp<arr[j]:
					arr[j+1]=arr[j]
					index=j
			arr[index]=temp
			
	print(arr)

## 归并排序MergeSort
### 归并排序是采用分治法的一个非常典型的应用。归并排序的思想就是先递归分解数组，再合并数组。

## 快速排序QuickSort
### 步骤：
1. 从数列中挑出一个元素作为基准数。
2. 分区过程，将比基准数大的放到右边，小于或等于它的数都放到左边。
3. 再对左右区间递归执行第二步，直至各区间只有一个数。
### Python3实现：
	def quick_sort(ary):
		return qsort(ary, 0, len(ary) - 1)


	def qsort(ary, left, right):
	    if left >= right:
	        return ary
	    key = ary[left]
	    lp = left
	    rp = right
	    while lp < rp:
	        while ary[rp] >= key and lp < rp:
	            rp -= 1
	        while ary[lp] <= key and lp < rp:
	            lp += 1
	        ary[lp], ary[rp] = ary[rp], ary[lp]
	    ary[left], ary[lp] = ary[lp], ary[left]
	    qsort(ary, left, lp - 1)
	    qsort(ary, rp + 1, right)
	    return ary
	
	
	ary = [1, 7, 4, 10, 6, 5]
	quick_sort(ary)
	print(ary)

### 面试题：
### N级台阶（比如100级），每次可走1步或者2步，求总共有多少种走法？
	stairs = 100


	def up_stairs(stair):
	    if stair <= 0:
	        return 0
	    if stair == 1:
	        return 1
	    if stair == 2:
	        return 2
	    return up_stairs(stair - 1) + up_stairs(stair - 2)
	
	
	print(up_stairs(stairs))

### 本文为<a href="http://blog.fishc.com/category/structure">小甲鱼《数据结构和算法》</a>的读书笔记
